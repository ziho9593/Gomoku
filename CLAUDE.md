# Gomoku Flutter Project

## 프로젝트 목적

본 프로젝트는 Flutter 개발 감각을 회복하고, 모바일 앱 개발의 기본 흐름을 익히기 위한 오목(Gomoku) 토이 프로젝트이다.

최종 목표는 로컬에서 실행 가능한 2인용 오목 게임을 만드는 것이다.

## 개발 환경

* OS: Windows
* Editor: VSCode
* Framework: Flutter
* Language: Dart
* Target: Android Emulator 우선
* Emulator: Medium Phone API 35 / Android 15.0

## 개발 원칙

* 초보자도 이해 가능한 코드 작성
* 한 번에 복잡한 구조를 만들지 않는다
* 먼저 동작하게 만들고, 이후 리팩토링한다
* 상태 관리는 초기에는 `StatefulWidget`과 `setState()`만 사용한다
* Provider, Riverpod, Bloc 등 외부 상태관리 라이브러리는 초기 버전에서 사용하지 않는다
* Codex가 코드를 수정할 때 기존 동작을 깨지 않도록 한다
* 기능 추가 전후로 반드시 실행 확인한다

## 1차 구현 범위

로컬 2인용 오목 게임을 구현한다.

필수 기능:

* 15x15 오목판 표시
* 흑돌/백돌 번갈아 착수
* 이미 돌이 놓인 위치에는 착수 불가
* 현재 차례 표시
* 가로, 세로, 대각선 5목 승리 판정
* 승리 후 추가 착수 방지
* 흑돌 금수 방지
* 무르기
* 게임 재시작 버튼

## 제외 범위

초기 버전에서는 다음 기능을 구현하지 않는다.

* 온라인 대전
* 로그인
* 서버 연동
* Firebase 연동
* AI 대전
* 랭킹 시스템
* 복잡한 애니메이션
* 과도한 디자인 시스템
* 외부 상태관리 라이브러리

## 권장 프로젝트 구조

초기에는 `main.dart` 하나에서 시작해도 된다.

현재 프로젝트는 기본 기능이 안정화되어 아래 구조로 분리한 상태다.

```text
lib/
 ├─ main.dart
 ├─ screens/
 │   └─ game_screen.dart
 ├─ widgets/
 │   ├─ gomoku_board.dart
 │   └─ game_status_bar.dart
 ├─ models/
 │   └─ board_point.dart
 └─ logic/
     └─ game_rules.dart
```

새 기능을 추가할 때는 가능한 한 이 구조를 유지한다.

## 데이터 표현 방식

오목판은 2차원 리스트로 관리한다.

```dart
List<List<int>> board;
```

값의 의미:

```text
0 = 빈 칸
1 = 흑돌
2 = 백돌
```

현재 차례는 정수 또는 enum으로 관리할 수 있다.

초기 구현에서는 단순성을 위해 정수를 사용해도 된다.

## 승리 판정 기준

착수 후 마지막으로 둔 돌을 기준으로 4개 방향을 검사한다.

```text
가로:       (0, 1)
세로:       (1, 0)
대각선 ↘:   (1, 1)
대각선 ↗:   (1, -1)
```

각 방향에서 양쪽으로 같은 색 돌의 개수를 세고, 총합이 5 이상이면 승리로 판단한다.

현재 버전에서는 흑돌에 한해 장목, 쌍삼, 쌍사 금수를 준렌주룰 방식으로 방지한다.
대회용 렌주룰 100% 구현은 아니지만, 단순 문자열 패턴보다 실제 5목/열린 4 확장 가능성을 더 넓게 검사한다.

## UI 구현 방향

* 오목판은 `CustomPainter` 또는 `GridView`로 구현한다
* 초기에는 구현이 쉬운 방식을 우선한다
* 터치 좌표를 판 좌표로 변환해 돌을 둔다
* 화면 크기에 따라 오목판이 적절히 정사각형으로 표시되도록 한다
* Android Emulator에서 세로 화면 기준으로 우선 구현한다

## 코드 스타일

* Dart 기본 포맷터를 따른다
* 의미 있는 변수명을 사용한다
* 불필요하게 추상화하지 않는다
* 함수는 하나의 역할만 하도록 작성한다
* 게임 로직과 UI 코드는 가능하면 분리한다
* 초반에는 주석을 충분히 작성한다

## Git 커밋 컨벤션

커밋 메시지는 Gitmoji + Conventional Commits 형식을 사용한다.

형식:

```text
<gitmoji> <type>: <message>
```

예시:

```text
✨ feat: 기본 오목판 틀 추가
🐛 fix: 점유된 자리에 착수 금지
♻️ refactor: 승리 판정 로직 개선
💄 style: 오목판 레이아웃 변경
📝 docs: 프로젝트 가이드 수정
```

주요 타입:

```text
feat     새로운 기능
fix      버그 수정
refactor 리팩토링
style    UI/스타일 변경
docs     문서 변경
test     테스트 추가/수정
chore    설정, 빌드, 기타 작업
```

## Codex 작업 지침

Codex는 다음 원칙을 따른다.

* 한 번에 하나의 기능만 구현한다
* 변경 범위를 작게 유지한다
* 실행 가능한 코드를 우선 작성한다
* Flutter/Dart 초보자가 이해할 수 있도록 설명 가능한 구조로 작성한다
* 불필요한 패키지를 추가하지 않는다
* 패키지 추가가 필요하면 먼저 이유를 설명한다
* 기존 기능을 삭제하거나 크게 바꾸기 전에 확인한다

## Codex에게 줄 수 있는 예시 프롬프트

```text
Flutter로 로컬 2인용 오목 게임을 만들고 싶다.

우선 main.dart 하나에 15x15 오목판을 그리고,
사용자가 칸을 탭하면 흑돌과 백돌이 번갈아 놓이게 해줘.

조건:
- StatefulWidget과 setState만 사용
- 외부 패키지 사용 금지
- Android Emulator 세로 화면 기준
- 초보자가 이해할 수 있도록 주석 작성
- 아직 승리 판정은 넣지 말 것
```

## 개발 단계

### Step 1. 기본 앱 실행

* Flutter 기본 Counter App 실행 확인
* Android Emulator 실행 확인
* `flutter doctor` 문제 확인

### Step 2. 오목판 표시

* 15x15 판 표시
* 화면 중앙 정렬
* 정사각형 유지

### Step 3. 돌 놓기

* 탭 위치 계산
* 흑/백 번갈아 착수
* 중복 착수 방지

### Step 4. 승리 판정

* 마지막 착수 위치 기준 검사
* 5목 이상이면 승리 처리
* 승리 메시지 표시

### Step 5. 게임 재시작

* 보드 초기화
* 현재 차례 초기화
* 승자 상태 초기화

### Step 6. 리팩토링

* UI 위젯 분리
* 게임 로직 분리
* enum 도입 검토
* 테스트 코드 추가 검토

## 주의사항

* 처음부터 완벽한 구조를 만들려고 하지 않는다
* 에러가 나면 에러 메시지를 그대로 확인하고 수정한다
* Codex가 제안한 코드는 반드시 직접 실행해본다
* 이해하지 못한 코드는 바로 복붙하지 않고 설명을 요청한다
* 현재 목표는 출시가 아니라 Flutter 기본기 회복이다
